import 'package:flutter/material.dart';

class HoriDivision extends StatelessWidget {
  const HoriDivision({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: 1,
    );
  }
}
